/**
 * - vr
 *   + rhythm
 *   + establishBaseline
 *   + linesForFontSize
 *   + adjustFontSizeTo
 *   + scale
 * - options
 * - styles
 */
export default (vr, options, styles) => {
  // console.log(vr)
  // console.log(options)
  // console.log(styles)
  return {}
}
